# pyFilterBox — 이미지 필터 변환기

Python 기반의 간단한 이미지 필터링 툴입니다. <br/>
업로드한 이미지를 흑백, 세피아, 스케치, 블러 등 다양한 스타일로 변환할 수 있습니다. <br/>
Streamlit 기반으로 웹 UI가 자동 생성되어 로컬에서 바로 실행하여 사용 가능합니다.

Installation

1. 저장소 클론
```
git clone https://github.com/Choi-jae-min/pyFilterBox.git
cd pyFilterBox
```
2. 가상환경 생성 & 활성화
```
python3 -m venv .venv
source .venv/bin/activate  # MacOS/Linux
```
4. 패키지 설치
```
pip install -r requirements.txt
```
🚀 Run App
```
streamlit run app.py
```

Pull Request, Issue 환영합니다.
